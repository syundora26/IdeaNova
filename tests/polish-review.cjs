const fs=require('fs');const{chromium}=require('@playwright/test');const{pathToFileURL}=require('url');const path=require('path');
(async()=>{const dir='qa/reference-polish/comparison';const points=JSON.parse(fs.readFileSync('qa/reference-polish/subject-landmarks.json','utf8')).points;
const marks=(kind)=>points.map((p,i)=>{const q=p[kind];return `<span class="mark" title="${p.name}" style="left:${q[0]/10.24}%;top:${q[1]/10.24}%">${i+1}</span>`}).join('');
let html=`<!doctype html><html lang="ja"><meta charset="utf-8"><title>IdeaNova 修正前後の比較</title><style>*{box-sizing:border-box}body{margin:0;background:#eee9df;color:#213c30;font:16px/1.8 sans-serif}header,section{padding:24px}h1{margin:0;font-size:28px}h2{font-size:22px}p{max-width:1100px}.triple{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}.pair{display:grid;grid-template-columns:1fr 1fr;gap:24px}figure{margin:0;min-width:0}figcaption{background:#fff;padding:10px;font-weight:bold}img{width:100%;height:auto;display:block}.photo{position:relative}.marks{position:absolute;left:0;top:0;width:100%;aspect-ratio:1;pointer-events:none;display:none}.mark{position:absolute;border:1px solid #fff;border-radius:50%;background:#a44332;color:#fff;font:bold 10px/18px sans-serif;width:20px;height:20px;transform:translate(-50%,-50%);text-align:center}.marked .marks{display:block}button,a{padding:10px 18px;color:inherit;font:inherit}table{border-collapse:collapse;background:#fff}td,th{padding:8px 16px;border-bottom:1px solid #ddd;text-align:left}button:focus-visible,a:focus-visible{outline:3px solid #a44332}</style><header><h1>IdeaNova — 参考画像・修正前・修正後</h1><p>画像・見出し・ボタンは独立したUIのまま、全幅時の比率、曲線の境界、石台の接続、人物の位置を修正しました。参考の写真そのものとの画素一致ではありません。</p><a href="index.html">6セクションを大きく比較する</a></header><section id="wide"><h2>1888pxの大画面で比較（同じ表示幅に縮小）</h2><div class="triple">${[['../../../docs/reference/botanical-design.jpg','参考：IMG_7664.jpg'],['../before/1888-home.png','修正前：高さの拡大が1440pxで停止'],['../after/1888x1000-home.png','修正後：幅・高さを同じ倍率で拡大']].map(([src,label])=>`<figure><figcaption>${label}</figcaption><img src="${src}" alt="${label}"></figure>`).join('')}</div></section><section id="landmarks"><h2>1024pxで被写体の目印を比較</h2><p>番号は目・鼻・唇・顎と商品・石台の9点。参考・素材上の位置を手作業で読み取った概算値です（読取り誤差 約3〜5px）。全輪郭の一致率ではありません。</p><button type="button" id="toggle" aria-pressed="false">目印を表示・非表示</button><div class="pair">${[['../../../docs/reference/botanical-design.jpg','参考画像','reference'],['../after/1024x900-home.png','修正後','actual']].map(([src,label,kind])=>`<figure><figcaption>${label}</figcaption><div class="photo"><img src="${src}" alt="${label}"><div class="marks">${marks(kind)}</div></div></figure>`).join('')}</div><table><tr><th>目印</th><th>参考 x, y</th><th>修正後 x, y</th><th>差 x, y</th></tr>${points.map((p,i)=>`<tr><td>${i+1}. ${p.name}</td><td>${p.reference.join(', ')}</td><td>${p.actual.join(', ')}</td><td>${p.delta.join(', ')}</td></tr>`).join('')}</table></section><script>document.getElementById('toggle').onclick=function(){const state=document.getElementById('landmarks').classList.toggle('marked');this.setAttribute('aria-pressed',state)}</script></html>`;
fs.writeFileSync(dir+'/review.html',html);const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1584,height:1000}});await p.goto(pathToFileURL(path.resolve(dir+'/review.html')).href);await p.evaluate(()=>Promise.all([...document.images].map(i=>i.decode())));await p.locator('#wide').screenshot({path:dir+'/before-after-wide.png'});await b.close();console.log('Comparison review created');})()

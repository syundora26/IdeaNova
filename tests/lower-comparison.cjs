const fs=require('fs');const path=require('path');const{chromium}=require('@playwright/test');const dir='qa/lower-finish/comparison';fs.mkdirSync(dir,{recursive:true});const metrics=JSON.parse(fs.readFileSync('qa/lower-finish/lower-results.json'));const m=metrics.sizes.find(s=>s.width===1024);const h=m.footer.bottom-m.values.y;
const crop=(src,y,height,normalizedWidth=1024,x=0,regionWidth=1024)=>`<div class="crop" style="aspect-ratio:${regionWidth}/${height}"><img src="${src}" style="width:${normalizedWidth/regionWidth*100}%;top:${-y/height*100}%;left:${-x/regionWidth*100}%" alt="比較記録"></div>`;
const col=(title,body)=>`<figure><figcaption>${title}</figcaption>${body}</figure>`;
const html=`<!doctype html><html lang="ja"><meta charset="utf-8"><title>IdeaNova 下部の装飾と輪郭の比較</title><style>*{box-sizing:border-box}body{margin:0;color:#213c30;background:#f8f5ee;font:16px/1.8 system-ui,sans-serif}main{max-width:1700px;margin:auto;padding:30px}h1{font-size:28px}h2{font-size:22px}section{padding:24px 0;border-top:1px solid #213c3030}.columns{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}figure{margin:0}figcaption{font-weight:600;padding-bottom:12px}.crop{position:relative;overflow:hidden}.crop img{position:absolute;max-width:none;height:auto}.record{width:100%;height:auto}a{color:inherit}@media(max-width:850px){.columns{grid-template-columns:1fr}}</style><main><h1>価値観・お知らせ・問い合わせの修正</h1><p>参考画像の下部を同じ縮尺で比較。実際の記事、文字サイズ、法務・確認中表記による高さの違いは維持しています。</p><section id="lower"><h2>元画像・修正前・修正後</h2><div class="columns">${col('元画像：価値観から下',crop('../../../docs/reference/botanical-design.jpg',1100,436))}${col('修正前：価値観から下',crop('../before/1024-home.png',m.values.y,h))}${col('修正後：価値観から下',`<img class="record" src="../after/1024-lower.png" alt="修正後の下部">`)}</div></section><section id="right"><h2>右端の装飾と問い合わせ</h2><div class="columns">${col('元画像',crop('../../../docs/reference/botanical-design.jpg',1100,436,1024,740,284))}${col('修正前（1887px）',crop('../before/1887-home.png',metrics.sizes.find(s=>s.width===1887).values.y,733,1887,1327,560))}${col('修正後（1887px）',`<img class="record" src="../after/1887-right-detail.png" alt="葉・布・ガラスと問い合わせの輪郭">`)}</div></section><section><h2>画面記録</h2><p>${[1024,1440,1887,1920,768,390,320].map(w=>`<a href="../after/${w}-lower.png">${w}px</a>`).join(' ／ ')}</p><p><a href="../../../docs/LOWER_FINISH.md">実装・素材・検証結果</a></p></section></main></html>`;fs.writeFileSync(dir+'/index.html',html);
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1680,height:1080}});await p.goto('file:///'+path.resolve(dir,'index.html').replaceAll('\\','/'));await p.evaluate(async()=>Promise.all([...document.images].map(i=>i.decode())));for(const id of['lower','right'])await p.locator('#'+id).screenshot({path:dir+'/'+id+'.png'});await b.close()})()
